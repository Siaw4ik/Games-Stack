export type Settings = {
  lang: string;
  style: string;
  volume: boolean;
};

export type TypeUser = {
  username: string;
  password: string;
};

export type AnswerRecordUser = {
  success: boolean;
  message: string;
};

export type LogName = {
  username: string;
};
